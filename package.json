{
  "name": "my-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build_wasm": "cd ./src/wasm_src && wasm-pack build --release",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.3.4"
  },
  "dependencies": {
    "mobx": "^6.13.1",
    "vite-plugin-wasm": "^3.3.0"
  }
}
